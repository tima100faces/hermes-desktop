# HANDOFF — контекст для следующей сессии

> Обновлено: 2026-07-10. После выполнения задач — актуализировать этот файл
> (вычеркнуть сделанное, дописать новый контекст).

## Где мы сейчас

Завершён большой редизайн (стиль Obsidian + ржавый акцент) и переход на
Claude Code как основной инструмент разработки. Сборка проверена:
`make build` и `make run` проходят, приложение запускается.

Сделано в последних сессиях (всё в main):
- Дизайн-система на hex-токенах, ржавая пара #b7410e / #d97e52
- Анатомия сообщений (пользователь в карточке, агент плоским markdown)
- Копирование: hover-ряд иконок + выделение текста; блоки кода с Copy
- Растущий инпут (Enter / Shift+Enter)
- Настройки → General → Agent name; футер с живым health-check (ConnectionMonitor)
- ThinkingIndicator (пульсирующая точка «думает») + автовыбор последнего
  проекта при запуске — оба подтверждены владельцем визуально, работают

В ветке `feature/project-management` (пока не в main, ждёт подтверждения
билда владельцем перед мержем):
- Починен баг: `lastActiveAt` раньше выставлялся только при создании
  проекта и больше никогда не обновлялся — «последний активный» на деле
  был «последний созданный». Теперь обновляется при каждой отправке и
  получении сообщения (ChatViewModel).
- Управление проектами: иконка «…» в строке проекта (по наведению) +
  правый клик — меню Rename / Delete. Rename — модальное окно
  (RenameProjectSheet), Delete — как раньше, с подтверждением. Владелец
  проверил вживую — работает.
- Архивирование обсудили и осознанно решили не делать (см. UI-SPEC §10,
  «Решено и закрыто»).
- Починен баг автоскролла: при отправке сообщения скролл иногда
  оставался на последнем сообщении, а точка «думает» уезжала ниже
  экрана (гонка между двумя `onChange`). Теперь автоскролл при отправке
  всегда целится в индикатор стриминга, если run активен.
- Добавлена кнопка «прокрутить вниз» (`ScrollToBottomButton`, ChatView.swift):
  появляется, когда чат прокручен вверх, стоит над кнопкой отправки.
  Владелец проверил вживую — работает.

## Сначала проверить (5 минут)

Ничего не висит — всё из этой ветки (пульсация, автовыбор проекта,
rename/delete, автоскролл, кнопка «вниз») владелец подтвердил лично.

## Беклог — по приоритету

Брать по одной задаче за раз, каждая — в своей feature-ветке.

### 1. Retry в hover-ряду
Иконка `arrow.clockwise` (IconActionButton) в hover-ряду ПОСЛЕДНЕГО ответа
ассистента. Клик → повторная отправка последнего СООБЩЕНИЯ ПОЛЬЗОВАТЕЛЯ
(нужен метод в ChatViewModel; старый ответ НЕ удалять, новый приходит
следующим). Стиль кнопки — как у Copy (UI-SPEC §5).

### 2. Cmd+K — палитра проектов
Оверлей по центру окна: поле поиска + список проектов, фильтр по вводу,
стрелки + Enter для выбора, Esc — закрыть. Стиль: карточка `hkSurface`,
радиус 12, `hkGlowStrong`, ширина ~480, затемнение фона black 40%.
Выбранный элемент — подложка `hkAccentDim` (как в сайдбаре).
Это единственное допустимое исключение из правила «только inset glow»:
оверлею можно мягкую тень `hkPage` для отделения от фона — но сначала
показать владельцу вариант без тени.

### 3. Shift+Enter по позиции курсора
Сейчас перенос вставляется в конец текста (ограничение SwiftUI
TextField). Решение — NSTextView-обёртка (NSViewRepresentable). Объёмная
задача, не совмещать с другими.

### 4. Подсветка синтаксиса в CodeBlockView
Минималистичная: ключевые слова — `hkAccent2`, строки — `hkSuccess`,
комментарии — `hkNeutral`, остальное — `hkMuted`. Без тяжёлых зависимостей;
если без библиотеки никак — сначала обсудить с владельцем.

## Правила (коротко; полностью — CLAUDE.md и docs/UI-SPEC.md)

- Перед любым UI-кодом — прочитать UI-SPEC.md, в конце — чек-лист.
- Работа в feature-ветках; в main — только после подтверждения владельца.
- После изменений — `make build`; если менялся UI — обновить спеку.
- Решения из «Решено и закрыто» (UI-SPEC §10) не пересматривать.
- Владелец — дизайнер, не программист: объяснять просто, по-русски,
  визуальные изменения описывать до реализации.
